{
  "name": "swallow-cli",
  "description": "A migration utility for AWS resources",
  "version": "1.0.8",
  "author": "Ben Force",
  "bin": {
    "swallow": "./bin/run"
  },
  "bugs": {
    "url": "https://github.com/drg-adaptive/swallow-migration/issues"
  },
  "dependencies": {
    "@oclif/command": "^1",
    "@oclif/config": "^1",
    "@oclif/plugin-help": "^2",
    "@types/node": "^10",
    "date-fns": "^2.0.1",
    "listr": "^0.14.3",
    "swallow-migration": "^1.0.8",
    "tslib": "^1",
    "zen-observable": "^0.8.14"
  },
  "devDependencies": {
    "@oclif/dev-cli": "^1",
    "@oclif/tslint": "^3",
    "@types/date-fns": "^2.6.0",
    "@types/listr": "^0.14.2",
    "@types/zen-observable": "^0.8.0",
    "globby": "^10",
    "ts-node": "^8.3.0",
    "tslint": "^5",
    "typescript": "^3.3"
  },
  "engines": {
    "node": ">=8.0.0"
  },
  "files": [
    "/bin",
    "/lib",
    "/npm-shrinkwrap.json",
    "/oclif.manifest.json"
  ],
  "homepage": "https://github.com/drg-adaptive/swallow-migration",
  "keywords": [
    "oclif"
  ],
  "license": "MIT",
  "main": "lib/index.js",
  "oclif": {
    "commands": "./lib/commands",
    "bin": "swallow",
    "plugins": [
      "@oclif/plugin-help"
    ]
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/drg-adaptive/swallow-migration.git"
  },
  "scripts": {
    "postpack": "rm -f oclif.manifest.json",
    "posttest": "tslint -p . -t stylish",
    "prepack": "rm -rf lib && tsc -b && oclif-dev manifest && oclif-dev readme",
    "test": "echo NO TESTS",
    "version": "oclif-dev readme && git add README.md",
    "build": "rm -rf lib && tsc -b && oclif-dev manifest && oclif-dev readme"
  },
  "types": "lib/index.d.ts",
  "directories": {
    "lib": "lib",
    "test": "__tests__"
  },
  "publishConfig": {
    "registry": "https://registry.yarnpkg.com"
  }
}
