{
  "name": "dm-driver-lambda",
  "version": "1.3.0",
  "description": "Data Migration driver to update lambda properties like environment variables",
  "main": "lib/index.js",
  "repository": "git@github.com:theBenForce/data-migration.git",
  "author": "Ben Force",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "prepublish": "yarn build",
    "release": "semantic-release -e semantic-release-monorepo --tag-format='dm-driver-lambda@${version}'",
    "lint": "eslint src/**/*.*"
  },
  "dependencies": {
    "aws-sdk": "^2.655.0",
    "data-migration": "^2.10.0",
    "rxjs": "^6.5.5"
  },
  "files": [
    "/lib"
  ],
  "devDependencies": {
    "typescript": "^3.8.3"
  }
}
